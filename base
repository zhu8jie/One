gz压缩日志的检索
zgrep 匹配文本 <文件>

wc
wc -l		#统计行数
wc -c		#统计字节数
wc -w		#统计字数

grep -v 		  #排除匹配        cat xxx.txt | grep -v 8080
head -99		  #得到头部默认10条 这里得到成99条     cat xxx.txt | head -20

ps -aux      查看进程
netstat -apn     查看端口

find
find / -mtime -1 -print  一日以内的。包括修改的和创建的。
find / -mtime +1 -print 一日以前的。
 
ssh
/etc/rc.d/init.d/sshd restart			重启ssh服务


linux学习
http://www.study-area.org/linux/linuxfr1.htm
